version: "3.8"

services:
  nexy-mcp:
    image: keygray1610/nexy-mcp:v0.0.1
    platform: linux/amd64
    ports:
      - "3333:3333"
    environment:
      - NODE_ENV=production
    volumes:
      - /var/run/tappd.sock:/var/run/tappd.sock
    restart: unless-stopped
